/**
 * Admin Login Layout
 * Epic 6, Story 6.5: Admin Dashboard Authentication
 *
 * Simple layout for login page without authentication check
 */

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
